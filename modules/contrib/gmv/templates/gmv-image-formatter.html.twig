{{ attach_library('gmv/gmv_files') }}
<model-viewer src="{{ uri }}" {% if camera == 1 %} camera-controls {% endif %} shadow-intensity="{{ shadow }}" touch-action="{{ touch_action }}" skybox-image="{{ skybox_image }}" ></model-viewer>
